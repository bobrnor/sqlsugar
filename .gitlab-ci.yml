image: golang

before_script:
  - export CURRENT_BUILD_PATH=$(pwd)
  - echo $PATH
  - rm -rf $HOME/gopath
  - mkdir -p $HOME/gopath/src/git.nulana.com/bobrnor/sqlsugar.git
  - export GOPATH=$HOME/gopath
  - export PATH=$PATH:$GOPATH/bin
  - go version
  - echo $PATH
  - go env
  - which go

test:
  script:
    - echo $CURRENT_BUILD_PATH
    - ls -l
    - cp -R $CURRENT_BUILD_PATH/ $HOME/gopath/src/git.nulana.com/bobrnor/sqlsugar.git/
    - cd $HOME/gopath/src/git.nulana.com/bobrnor/sqlsugar.git/
    - ls -l
    - go get ...
    - go test
